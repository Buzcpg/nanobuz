# NanoClaw Configuration
# Copy this file to .env and fill in your values

# Claude API authentication (one of these is required)
ANTHROPIC_API_KEY=sk-ant-...
# OR use OAuth token:
# CLAUDE_CODE_OAUTH_TOKEN=...

# Assistant name (appears in messages, used as trigger)
ASSISTANT_NAME=Andy

# Discord configuration (required)
DISCORD_BOT_TOKEN=your-bot-token
DISCORD_GUILD_ID=your-server-id

# Logging level (debug, info, warn, error)
LOG_LEVEL=info

# Optional: Container timeout in ms (default: 300000 = 5 minutes)
# CONTAINER_TIMEOUT=300000

# Optional: Timezone for scheduled tasks (default: system timezone)
# TZ=America/New_York
